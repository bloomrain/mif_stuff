require 'spec_helper'

describe Alignment::Strategy::Blind do
  describe "probablility" do
    context "of first paragraph" do
      it "is always 100%"
    end

    context "of last paragraph" do
      it "is always 100%"
    end
  end
end
